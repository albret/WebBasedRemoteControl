use rcdb

drop table users;

create table users(
	id INT NOT NULL AUTO_INCREMENT,
	username CHAR(50),
	email CHAR(100),
	hash CHAR(64),
	active BOOLEAN NOT NULL DEFAULT 1,
	PRIMARY KEY (id)
);

show tables;
